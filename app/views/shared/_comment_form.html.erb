<div class="comment_form hidden">
  <%= form_for :comment, url: post_comments_path(answer), remote: true do |f| %>
    <%= f.label :body %><br>
    <%= f.text_area :body, required: true %><br>
    <%= f.submit %>
  <% end %>
</div>